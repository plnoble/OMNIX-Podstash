Sideload signing key for GitHub Releases (PKCS12).

- file: `omnix-upload.p12`
- alias: `omnix`
- password: `omnix-podstash`

This key is committed so CI can produce APKs that the in-app updater can install over previous builds. It is not a Play App Signing key.
