# OMNIX-Podstash 下一期开发计划（v0.2）

当前已发布：**0.1.1**。v0.2 开发中（暂停下载、目录、多选、听库打磨、OPML）。  
仓库：https://github.com/plnoble/OMNIX-Podstash  
工程：`D:\Agent\Project\Download\播客`

用户为本期选定：

1. **把 Android 听库打磨到能天天用**
2. **OPML 导入 / 导出**

本期不做（已明确后置）：PC 订阅自动下新集、PC 改独立窗口 exe、只修 PC 下载。

---

## 0.1.1 现状（缺口）

已经有：搜索、热门、关注、下载、跳过已有、打开 App 下新集、简单播放、GitHub 更新弹窗。

日常听还缺：

- 播放页不完整：进度条、拖动、倍速入口不明显、没有睡眠定时
- 「继续听」弱：进度写入不稳，杀进程后可能丢
- 下载：通知、失败重试、仅 Wi-Fi、队列一眼能看懂
- 订阅：打开即检查还行，失败提示不清楚；大列表（200+ 集）可能卡
- 库首页：已下载 / 已关注 / 未听完 分得不够清
- 真机：后台播放、通知权限、安装未知应用、存储路径

OPML：两端都还没有。

---

## v0.2 目标

手机打开就能接着听；换客户端时用 OPML 把订阅迁进迁出。版本号 **0.2.0**（Android `versionCode` 在 0.1.1 的 2 上继续加）。

---

## A. Android 听库打磨

按使用顺序，而不是按文件。

### A1. 播放器（天天听的核心）

- 迷你条 + 全屏播放：封面、节目名、单集名
- 进度条可拖；显示已播 / 剩余
- 倍速 0.8 / 1.0 / 1.2 / 1.5 / 2.0，记住每个用户全局（可后补「按节目」）
- 跳过 ±10 / ±30 秒
- **睡眠定时**：15 / 30 / 45 / 60 分钟，到点暂停
- 锁屏 / 通知栏控件（Media3 session，补稳）
- 进度：暂停、切集、退出时写入；启动「继续听」回到上次那一集 + 位置

### A2. 库首页

- 继续听（大卡片）
- 未听完
- 已关注（角标：有新集）
- 已下载
- 空状态：引导去「发现」关注

### A3. 下载与订阅

- 下载队列界面：等待 / 进行中 / 失败；失败一键重试
- 仅 Wi-Fi 开关生效且有说明
- 打开 App：检查订阅，只下 **比 lastSeen 更新的未下载集**（不要整档重来）
- 检查失败：明确「某节目 RSS 失败」，不要静默当成功
- 200+ 集列表：懒加载或复用，不要一次卡死

### A4. 设置与更新

- 显示版本、存储位置（应用专属目录说明白）
- 检查更新沿用 GitHub Release
- 通知权限引导（Android 13+）

### A5. 真机验收清单

- 后台听 10 分钟不被杀（可开电池优化白名单说明）
- 断网时已下载能播
- 再打开进度还在
- 关注后第二次打开只下新集
- 更新弹窗能装（未知来源）

---

## B. OPML 导入 / 导出

标准：`<opml><body><outline type="rss" text="节目名" xmlUrl="feedUrl"/>`

兼容：Apple Podcasts、AntennaPod、Pocket Casts、小宇宙导出的常见 OPML。

### Android

- 设置或库页：**导入 OPML**（文件选择器）、**导出 OPML**（分享/保存）
- 导入：解析 `xmlUrl` → 写入已关注；可选「导入后立即检查新集」（默认只订不整档下载）
- 导出：所有 `subscribed == true` 的 feed
- 坏 feed：跳过并列出失败项

### PC（顺手做，迁订阅才完整）

- 网页：导入文件 / 导出下载 `podstash.opml`
- 同样：导入 = 关注，不自动下整档
- 与 Android 同一套字段（name, feedUrl, apple id 若有）

---

## 不做（本期）

- PC 订阅定时下新集、PC 独立 exe
- 局域网 PC↔手机听同一座库
- 转写、AI 摘要、账号云同步
- 改播放内核（继续 Media3）

---

## 建议提交顺序

1. 播放器进度 / 倍速 / 睡眠 / 继续听  
2. 库首页信息架构  
3. 下载队列 + 订阅检查提示  
4. Android OPML  
5. PC OPML  
6. 真机走验收清单  
7. `VERSION` → `0.2.0`，`versionCode` +1，打 tag 发 APK  

---

## 完成标准

- 能连续听一集、锁屏控、杀进程再开还能续  
- 睡眠定时可用  
- 导入一份 OPML 后库里出现对应关注；导出再导入不丢 feed  
- 发 GitHub Release `v0.2.0`，旧 0.1.1 能弹窗升上来  
