# OMNIX-Podstash · PC 库管

仓库根目录的说明见上一级 [README.md](../README.md)。

```bash
pip install -r requirements.txt
python app.py
```

或在仓库根目录双击 `启动.bat`。

设置与关注列表保存在 `~/.omnix-podstash/state.json`（Docker 里是 `/config/state.json`）。
