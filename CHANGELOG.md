# Changelog

## 0.4.6

- 新增设置「自动升级低质量」：检测到低码率文件时自动下载高质量版本替换（默认关）
- 替换流程安全：先下载到临时文件 → 校验时长完整且码率确实更高 → 才删除旧文件；失败则保留原文件

## 0.4.5

- 修复：已有文件改用「播放时长」判断是否完整（不再看体积），低码率完整文件不再被误判为未下完
- 残缺文件因播放时长偏短仍会被正确识别

## 0.4.4

- 修复：已有文件因体积校验过严被误判为未下载（低码率文件 vs 高码率 RSS 体积，如《日谈公园》）
- 检测到的文件实际体积会记入索引，自动扫描不再重复下载
- 完整性校验放宽：接受 ≥ 预期体积 10% 的低码率文件，仅拒绝明显残缺的下载

## 0.4.3

- 修复：同名节目重复关注（数据库主键 bug，导致两个《知行小酒馆》）
- 修复：RSS 节目主键存成空字符串导致节目互相覆盖
- 启动时自动修复历史脏数据：合并同名重复条目、补全空主键

## 0.4.2

- 新增：界面内「诊断」按钮——一键生成识别诊断（目录/文件/每集匹配分）并可一键复制，用于排查「检测已有文件」识别不到的问题

## 0.4.1

- 修复：打开节目加载慢（SQLite 批量写入 + WAL 去重 + shownotes 解析缓存）
- 修复：下载进度不可见（任务栏显示「正在下载 N 集 · 已用时」+ 单集「连接中…」）
- 修复：下载卡死（封面 15s 超时、CDN 无数据 120s 自动失败重试）
- 修复：同名节目出现两条（自动合并去重）
- 修复：已有文件识别不到（容器 chown 遍历整棵树，修属主不对的子文件）
- 新增：给已有文件补写标签按钮（标题/作者/专辑/音轨/封面）
- 新增：/api/scan-debug 诊断接口

## 0.4.0

Docker / 服务端主线：

- 更新：镜像发布到阿里云 ACR（国内直连、公开匿名拉取），`docker compose pull && docker compose up -d` 一键更新；网页顶部有新版本提示
- 数据层：`state.json` 升级为 SQLite（旧配置自动迁移），下载队列重启不丢、可恢复重试
- 镜像：`HEALTHCHECK`、非 root 运行（`PUID`/`PGID`）、`PODSTASH_PASSWORD` 单密码登录
- RSS 条件请求缓存（ETag / Last-Modified）；`GET /api/backup` 一键备份
- shownotes 存储与展示；单集内容全文搜索
- 下载写入 ID3/MP4 标签（封面/标题/节目名/集数）+ 文件名加集数编号
- 新增「只下新集 / 补齐缺失」开关；过滤 trailer / bonus 单集；单集「忽略」
- 每档独立扫描设置（间隔 / 每轮上限）；失败自动重试

Android：本版无改动（`versionCode` 保持 6，不弹更新）。

## 0.3.2

- Android：点开节目先进入页面再拉列表、再检测本地；「检测已有」有进度提示
- Android：库页空状态三步指引
- versionCode 6，应用内更新可弹出

## 0.3.1

- 首次打开三步指引；设置默认收起
- 点开节目先显示壳和列表，再后台检测已有文件，并有进度条提示
- 单集列表先渲染 40 集；定期扫描不卡住页面
- Docker 可显示真实「播客」目录

## 0.3.0

- 定期自动扫描：关注的节目按天/周/两周检查，补下还未下载的单集（可开关；每档每次集数可限制）
- PC / Docker：关注、OPML 导入导出、设置与订阅写入本地配置
- Docker：`docker-compose.yml` + 多架构镜像（linux/amd64、linux/arm64）

## 0.2.1

- 打开节目时扫描设定的下载目录（含子文件夹、你选择的系统文件夹）
- 文件名包含单集标题即视为已下载（支持「001 标题」「日期 标题」「节目名 - 标题」）
- PC / Android 增加「检测已有文件」；识别到的集不会重复下载，Android 可直接播本地文件

## 0.2.0

- Android：暂停 / 继续下载（断点续传）
- Android：设置里显示下载目录，可选择系统文件夹（完成后复制一份）
- Android：节目页多选后下载已选；仍保留「下载未有」
- Android：播放页进度条、倍速、±10/30 秒、睡眠定时、继续听
- Android：库首页下载队列；OPML 导入 / 导出

## 0.1.1

- 修复 Android 启动图标 XML，使 GitHub Actions 能打出 APK

## 0.1.0

- PC 库管：搜索、热门、全集下载、已有跳过；改名为 OMNIX-Podstash
- Android：搜索、关注、下载、播放、打开即下新集、GitHub 应用内更新
- 版本与 APK 由 GitHub Release 发布
